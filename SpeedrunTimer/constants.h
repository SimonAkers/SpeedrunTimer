#pragma once

namespace constants {
	const std::string cvar_timer_x = "timer_x";
	const std::string cvar_timer_y = "timer_y";

	const std::string cfg_file_name = "speedruntimer.cfg";

	// X offset of the text for alignment with the background box
	const int timer_text_xoff = 5;
	// Y offset of the text for alignment with the background box
	const int timer_text_yoff = 1;

	// Margin of pixels between the timer and the edge of the screen
	const int timer_margin = 5;
}